import SwiftUI

/// Foundry Clip design system colors.
/// Forge aesthetic: charcoal blacks, amber accents.
enum FoundryColors {
    /// Primary background — charcoal black
    static let background = Color(red: 0.078, green: 0.071, blue: 0.063)

    /// Control and secondary background — slightly lighter charcoal
    static let controlBackground = Color(red: 0.118, green: 0.110, blue: 0.098)

    /// Primary text — near-white
    static let textPrimary = Color(red: 0.92, green: 0.90, blue: 0.87)

    /// Secondary text — muted grey
    static let textSecondary = Color(red: 0.55, green: 0.52, blue: 0.47)

    /// Accent — amber forge glow
    static let accent = Color(red: 0.910, green: 0.659, blue: 0.286)
}
